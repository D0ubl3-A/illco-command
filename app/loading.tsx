export default function Loading() {
  return (
    <main className="fallbackPage">
      <section className="fallbackPanel panel">
        <span className="eyebrow">ILLCO Command</span>
        <h1>Loading the working app funnel.</h1>
        <p>Preparing subscription access, fast proof clips, and enterprise setup.</p>
      </section>
    </main>
  );
}
