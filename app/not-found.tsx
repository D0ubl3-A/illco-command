export default function NotFoundPage() {
  return (
    <main className="fallbackPage">
      <section className="panel fallbackPanel">
        <span className="brandGlyph">IC</span>
        <h1>That ILLCO Command route is not available.</h1>
        <p>Return to the main funnel to view working apps, fast demo videos, and enterprise setup options.</p>
        <a className="button primary" href="/">Open ILLCO Command</a>
      </section>
    </main>
  );
}
