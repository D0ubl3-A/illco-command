services:
  - type: web
    name: ai-project-kickoff-builder
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    disk:
      name: project-g-data
      mountPath: /app/data
      sizeGB: 1
